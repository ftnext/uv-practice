[project]
name = "mylib"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "taskipy>=1.14.1",
]
